package dangod.wechat.manager.message;

import dangod.wechat.core.model.message.req.VideoMessage;
import dangod.wechat.core.model.message.send.TextSend;
import org.springframework.stereotype.Component;

@Component
public class VideoMessageManager {
    public String getResulte(VideoMessage message){
        String result = null;
        return result;
    }
}
