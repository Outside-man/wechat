package dangod.wechat.manager.message;

import dangod.wechat.core.model.message.req.ImageMessage;
import dangod.wechat.core.model.message.send.TextSend;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

@Component
public class ImageMessageManager {
    public String getResulte(ImageMessage message) {
        String result = null;
        return result;
    }
}
